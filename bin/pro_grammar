#!/usr/bin/env ruby
# frozen_string_literal: true

# (C) John Mair (banisterfiend)
# MIT license

$0 = 'pro_grammar'

require 'pro_grammar'

# Process command line options and run ProGrammar
opts = ProGrammar::CLI.parse_options
ProGrammar::CLI.start(opts)
